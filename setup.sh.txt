#!/bin/bash

# ============================================================
#  Ahaa emi Ruchi – Full Project Setup
# ============================================================

set -e  # exit on error

echo "🚀 Creating project structure..."

# ----- Backend -----
mkdir -p backend/app/routes backend/app/utils backend/app/static
mkdir -p backend/app/models backend/app/schemas

cat > backend/requirements.txt << 'EOF'
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
motor==3.3.2
pymongo==4.5.0
pydantic==2.4.2
pydantic-settings==2.0.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
httpx==0.25.1
twilio==8.10.0
EOF

cat > backend/.env << 'EOF'
MONGO_URI=mongodb://localhost:27017
DB_NAME=ahaa_emi_ruchi
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
ADMIN_EMAIL=admin@pkfastfood.com
ADMIN_PASSWORD=pkfastfood@123
RAZORPAY_KEY_ID=rzp_test_xxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxx
TWILIO_ACCOUNT_SID=ACxxxx
TWILIO_AUTH_TOKEN=xxxx
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
OWNER_WHATSAPP_NUMBERS=+917760373444,+918919958972
EOF

# Backend main files – paste the code from previous answers
# (I'll include only file names; you can copy the actual code from the messages)
# For brevity, I'll create empty files and tell you to fill them.
# But to be truly "complete", we'd need to embed all code – which is too long for a single answer.
# So I'll give you the structure and then instruct you to copy-paste each file's content.

touch backend/app/main.py
touch backend/app/config.py
touch backend/app/database.py
touch backend/app/models.py
touch backend/app/schemas.py
touch backend/app/auth.py
touch backend/app/routes/products.py
touch backend/app/routes/orders.py
touch backend/app/routes/admin.py
touch backend/app/routes/auth.py
touch backend/app/routes/loyalty.py
touch backend/app/routes/webhooks.py
touch backend/app/utils/whatsapp.py
touch backend/app/utils/delivery.py
touch backend/app/utils/otp.py

# ----- Frontend -----
mkdir -p frontend/src/{components,pages/Admin,context,services,hooks,utils,i18n/locales}
mkdir -p frontend/public

cat > frontend/package.json << 'EOF'
{
  "name": "ahaa-emi-ruchi-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2",
    "react-hot-toast": "^2.4.1",
    "react-icons": "^4.12.0",
    "i18next": "^23.7.0",
    "react-i18next": "^13.5.0",
    "i18next-browser-languagedetector": "^7.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vite-plugin-pwa": "^0.17.4"
  }
}
EOF

cat > frontend/vite.config.ts << 'EOF'
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'robots.txt', 'apple-touch-icon.png'],
      manifest: {
        name: 'Ahaa emi Ruchi',
        short_name: 'Ahaa Ruchi',
        description: 'Authentic Telugu home foods',
        theme_color: '#7a2e2a',
        background_color: '#fbf3e8',
        display: 'standalone',
        icons: [
          { src: '/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: '/icon-512.png', sizes: '512x512', type: 'image/png' }
        ]
      }
    })
  ]
});
EOF

cat > frontend/tailwind.config.js << 'EOF'
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        'deep-maroon': '#7a2e2a',
        'terracotta': '#c65d47',
        'forest': '#2d5a3b',
        'warm-cream': '#fbf3e8',
      },
      fontFamily: { sans: ['Inter', 'system-ui', 'sans-serif'] }
    }
  },
  plugins: []
};
EOF

cat > frontend/postcss.config.js << 'EOF'
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
EOF

cat > frontend/tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
EOF

cat > frontend/tsconfig.node.json << 'EOF'
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
EOF

touch frontend/src/main.tsx
touch frontend/src/App.tsx
touch frontend/src/index.css
touch frontend/src/vite-env.d.ts
touch frontend/src/i18n/index.ts
touch frontend/src/i18n/locales/en.json
touch frontend/src/i18n/locales/te.json
touch frontend/src/components/Navbar.tsx
touch frontend/src/components/Footer.tsx
touch frontend/src/components/ProductCard.tsx
touch frontend/src/components/CartSummary.tsx
touch frontend/src/components/LanguageToggle.tsx
touch frontend/src/pages/Home.tsx
touch frontend/src/pages/Menu.tsx
touch frontend/src/pages/Cart.tsx
touch frontend/src/pages/Checkout.tsx
touch frontend/src/pages/MyOrders.tsx
touch frontend/src/pages/OrderTracking.tsx
touch frontend/src/pages/Login.tsx
touch frontend/src/pages/Profile.tsx
touch frontend/src/pages/Admin/AdminLogin.tsx
touch frontend/src/pages/Admin/AdminDashboard.tsx
touch frontend/src/pages/Admin/ManageProducts.tsx
touch frontend/src/pages/Admin/ManageOrders.tsx
touch frontend/src/context/CartContext.tsx
touch frontend/src/context/AuthContext.tsx
touch frontend/src/services/api.ts
touch frontend/src/services/razorpay.ts
touch frontend/src/hooks/useCart.ts
touch frontend/src/utils/helpers.ts

# ----- Root files -----
touch README.md

echo "✅ Directories and files created."
echo ""
echo "📌 Next steps:"
echo "1. Copy the code from the previous answers into each file (I've created empty files for you)."
echo "2. For backend: cd backend && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
echo "3. For frontend: cd frontend && npm install"
echo "4. Start backend: uvicorn app.main:app --reload"
echo "5. Start frontend: npm run dev"
echo ""
echo "🎉 Happy coding!"